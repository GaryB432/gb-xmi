/* This is a generated file. Make changes to cli.config.json and run "nx sync cli" */

import { showTsCommand } from './show-ts.command';

describe('ShowTs Command', () => {
  it('should create an instance', () => {
    expect(showTsCommand).toBeTruthy();
  });
});
